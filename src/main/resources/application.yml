server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/secured_app
    username: app
    password: Password123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: MYSQL
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      dialect: org.hibernate.dialect.MySQL8Dialect